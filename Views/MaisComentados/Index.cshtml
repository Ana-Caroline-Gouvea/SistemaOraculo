@model IEnumerable<Oraculo.Models.MaisComentados>

@{
    ViewData["Title"] = "Index";
}

<h1>Mais Comentados</h1>

<p>
    <a asp-action="Create" class="create">Criar Mais Comentados</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Postagem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Postagem.PostagemNome)
            </td>
            <td class="details">
                    <a asp-action="Details" asp-route-id="@item.MaisComentadosId"><i class="fa-solid fa-circle-info"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
