@model IEnumerable<Oraculo.Models.ComunidadeUsuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comunidades)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.UsuarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comunidades.ComunidadesId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ComunidadeUsuarioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ComunidadeUsuarioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ComunidadeUsuarioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
